# Версии рантаймов проекта. mise читает это на обеих машинах — и на маке,
# и в WSL, — поэтому «у меня собирается, у тебя нет» больше не случается.
#
# Flutter 3.44.2 закреплён намеренно: в нём IconData стал final, из-за чего
# отвалился заброшенный lucide_icons. Перешли на lucide_icons_flutter,
# и откат на более старый Flutter теперь сломает уже его.
[tools]
flutter = "3.44.2"
node = "22"
